"""
Core application configuration and utilities.

Typical responsibilities of `app.core`:
- Global application settings (e.g. Pydantic `BaseSettings` config)
- Security utilities (JWT handling, password hashing)
- Logging configuration
- Application-wide constants and enums

You can split these into multiple modules, for example:
- `config.py` for settings
- `security.py` for auth-related helpers
- `logging.py` for logging setup
"""

# Placeholder for future core modules.
# Example:
# from .config import settings

